# 阿里云模板 · PPT 生成契约

已把官方模板 `_中文浅色版_阿里云官方演示文档_.pptx` 处理成可直接用的内容模板
`template_aliyun.pptx`：保留全部品牌排版/配色/背景，挑出 7 个高频页型并 token 化。

## 用法
```bash
python render.py outline.json template_aliyun.pptx output.pptx
```
生成端只需产出 `outline.json`（下方契约），样式由模板保真带过来。

## 7 个页型与字段（**务必遵守字数上限，超长会撑爆版式**）

| type | 字段 | 字数上限 |
|------|------|---------|
| `cover` | title / subtitle / speaker / tagline | 标题≤16，副标题≤24，落款短 |
| `toc` | ch1_title…ch4_title / ch1_sub…ch4_sub | 章节标题≤8，副标题≤14（固定 4 章节）|
| `section` | section_title（含编号，如"01 业务背景"）/ section_desc | 标题≤12，描述≤24 |
| `kpi` | title / subtitle / k{1,2,3}v / k{1,2,3}u / k{1,2,3}l / k{1,2,3}d | 数值v≤4，单位u≤2，标签l≤8，描述d≤16 |
| `cards3` | title / subtitle / c{1,2,3}_title / c{1,2,3}_body | 卡片标题≤8，卡片正文≤70 |
| `image_text` | title / subtitle / body_title / body / r1_title / r1_body / r2_title / r2_body / `img:main` | body 与 r*_body **均限一行≤16**；`img:main` 填图片路径或公网 URL |
| `closing` | （无字段，固定端页） | — |

KPI 字段含义：`v`=数值、`u`=单位、`l`=上方小标签、`d`=下方圆角说明。
image_text 右卡是"标题+一行说明"三行结构：(body_title, body)、(r1_title, r1_body)、(r2_title, r2_body)。

## 给生成端（大模型）的提示词要点
1. 只输出 JSON；type 与字段名严格照上表；遵守字数上限，超长就精简或拆成多页。
2. 内容仅来自检索资料，数字不编造。
3. 图片先用占位符 `[IMG::xxx]`，后处理映射成真实路径再写进 `img:main`。
4. 一份汇报的典型结构：cover → toc → (section → 若干内容页) × N → closing。

## 如何扩展更多页型
本次只 token 化了 7 个页型。原模板还有时间轴、图标三栏、四图卡片、六项编号、
柱/折线图、架构图等。按 `convert_template.py` 里的套路即可加入：
1. 在原 `src.pptx` 里定位该页的形状（用脚本打印 shape 路径与文字）；
2. 用 `set_token()` 把填充点改成 `{{token}}`，图片框改名 `{{img:key}}`；
3. 在该页备注写 `TYPE=新页型名`，并把它并入模板。

注意：**图表页填的是数据不是文字**，需要改写图表 XML / 内嵌 xlsx，属于另一类工程，
不在本套占位符方案内；架构图节点过多，也不适合简单占位填充。
